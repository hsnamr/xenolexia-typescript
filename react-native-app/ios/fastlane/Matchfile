# Xenolexia Match Configuration
# https://docs.fastlane.tools/actions/match/

# Git repository to store certificates
# git_url("git@github.com:xenolexia/certificates.git")

# The default type (development, adhoc, appstore)
type("development")

# App identifier
app_identifier(["com.xenolexia.app"])

# Apple ID
# username("your@email.com")

# Team ID
# team_id("XXXXXXXXXX")

# Storage mode (:git, :google_cloud, :s3)
storage_mode("git")

# Readonly mode (recommended for CI)
readonly(true)

# Clone branch name (optional)
# git_branch("master")

# Skip confirmation prompts
force_for_new_devices(true)
