# Xenolexia Android App Configuration
# https://docs.fastlane.tools/actions/appfile/

# Package name
package_name("com.xenolexia.app")

# Path to service account JSON key file
# json_key_file("path/to/service-account.json")

# Or use environment variable
# ENV variables used:
# - GOOGLE_PLAY_JSON_KEY (path to JSON key file)
# - SUPPLY_JSON_KEY (alternative)
# - SUPPLY_JSON_KEY_DATA (JSON content directly)

for_platform :android do
  # Platform-specific configuration
end
